name: CI (Fast)

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      test_filter:
        description: "Optional VSTest filter for fast-lane test selection"
        required: false
        default: ""

permissions:
  contents: read

concurrency:
  group: ci-fast-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET_NOLOGO: "1"
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages

jobs:
  build_and_test:
    name: Fast build + tests (.NET 8)
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
          cache: true
          cache-dependency-path: |
            **/packages.lock.json
            **/*.csproj

      - name: Resolve fast lane filter
        shell: bash
        run: |
          TEST_FILTER="${{ github.event.inputs.test_filter || '' }}"
          DEFAULT_FAST_FILTER="(Lane=unit|Lane=integration)"

          if [ -n "$TEST_FILTER" ]; then
            echo "TEST_FILTER_ARG=--filter $TEST_FILTER" >> "$GITHUB_ENV"
          else
            echo "TEST_FILTER_ARG=--filter $DEFAULT_FAST_FILTER" >> "$GITHUB_ENV"
          fi

      - name: Restore
        run: dotnet restore TimefallDeck.sln

      - name: Build
        run: dotnet build TimefallDeck.sln --configuration Release --no-restore

      - name: Verify fast lanes have targeted tests
        shell: bash
        run: |
          verify_lane() {
            local lane="$1"
            local output
            output="$(dotnet test TimefallDeck.sln --configuration Release --no-build --list-tests --filter "Lane=$lane")"
            echo "$output"

            if ! echo "$output" | grep -Eq '^[[:space:]]*Game\.Tests\.'; then
              echo "No tests discovered for Lane=$lane"
              exit 1
            fi
          }

          verify_lane "unit"
          verify_lane "integration"

      - name: Verify deck edge-case tests are discoverable
        shell: bash
        run: |
          DECK_FILTER="(FullyQualifiedName~DeckCycleSystemTests.PlayerDeck_ReshufflesMultipleTimes_BeforeEnemy|FullyQualifiedName~DeckCycleSystemTests.EnemyDeck_ReshufflesMultipleTimes_BeforePlayer|FullyQualifiedName~DeckCycleSystemTests.PlayerReshuffles_EnemyNeverReshuffles|FullyQualifiedName~DeckCycleSystemTests.Reshuffle_WithEmptyDiscard_IsNoOp|FullyQualifiedName~DeckCycleSystemTests.BurnCount_Truncates_WhenDeckTooSmall|FullyQualifiedName~DeckCycleSystemTests.IndependentDeckCycles_RemainIndependentAcrossMultipleTurns)"

          output="$(dotnet test tests/Game.Tests/Game.Tests.csproj --configuration Release --no-build --list-tests --filter "$DECK_FILTER")"
          echo "$output"

          for test_name in \
            "Game.Tests.Game.DeckCycleSystemTests.PlayerDeck_ReshufflesMultipleTimes_BeforeEnemy" \
            "Game.Tests.Game.DeckCycleSystemTests.EnemyDeck_ReshufflesMultipleTimes_BeforePlayer" \
            "Game.Tests.Game.DeckCycleSystemTests.PlayerReshuffles_EnemyNeverReshuffles" \
            "Game.Tests.Game.DeckCycleSystemTests.Reshuffle_WithEmptyDiscard_IsNoOp" \
            "Game.Tests.Game.DeckCycleSystemTests.BurnCount_Truncates_WhenDeckTooSmall" \
            "Game.Tests.Game.DeckCycleSystemTests.IndependentDeckCycles_RemainIndependentAcrossMultipleTurns"
          do
            if ! echo "$output" | grep -Fq "$test_name"; then
              echo "Required deck edge-case test not discovered: $test_name"
              exit 1
            fi
          done

      - name: Run deck edge-case tests
        shell: bash
        run: |
          dotnet test tests/Game.Tests/Game.Tests.csproj \
            --configuration Release \
            --no-build \
            --verbosity normal \
            --filter "(FullyQualifiedName~DeckCycleSystemTests.PlayerDeck_ReshufflesMultipleTimes_BeforeEnemy|FullyQualifiedName~DeckCycleSystemTests.EnemyDeck_ReshufflesMultipleTimes_BeforePlayer|FullyQualifiedName~DeckCycleSystemTests.PlayerReshuffles_EnemyNeverReshuffles|FullyQualifiedName~DeckCycleSystemTests.Reshuffle_WithEmptyDiscard_IsNoOp|FullyQualifiedName~DeckCycleSystemTests.BurnCount_Truncates_WhenDeckTooSmall|FullyQualifiedName~DeckCycleSystemTests.IndependentDeckCycles_RemainIndependentAcrossMultipleTurns)"

      - name: Run tests
        shell: bash
        run: |
          dotnet test TimefallDeck.sln \
            --configuration Release \
            --no-build \
            --verbosity normal \
            --logger "trx;LogFileName=test-results.trx" \
            --results-directory ./artifacts/test-results \
            $TEST_FILTER_ARG

      - name: Upload test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fast-ci-test-artifacts
          path: |
            artifacts/test-results/**/*.trx
            artifacts/test-results/**
            **/TestResults/**
            **/coverage*/**
          if-no-files-found: ignore
